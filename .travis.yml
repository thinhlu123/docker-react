language: generic 
sudo: required
services:
    - docker
    
before_install:
    - docker build -t thinhlu123/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true thinhlu123/docker-react npm run test -- --coverage